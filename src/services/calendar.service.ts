import * as userModel from "../models/user.model.js"
import * as calendarModel from "../models/calendar.model.js"




export const identifyCalendarDiffs = (c1: calendarModel.Calendar, c2: calendarModel.Calendar) => {

}

export const cleanCalendar = (user: userModel.User) => {

}
